/**
 * We define the output paths for VueJS build, so that the generated
 * artifacts are placed at a proper location as expected by the
 * back-end flask web application that will serve this VueJS app.
 */
module.exports = {
    publicPath: "/reactiweb/"
}