{
    "maxRequestSizeMb": "5",
    "sessionTimeoutMin": "15",
    "//1. ": "The request URI path pattern for serving the static web content.",
    "staticFilesPath": "/reactiweb/*",

    "//2. ": "Folder under which the static web content files will be looked up. MUST match the corresponding value in gradle build script.",
    "staticWebDir": "static_content",
    "//3. ": "Base path to be prefixed for routes defined in routesConfigFile.",
    "baseUriPath": "/reactiweb",
    "defaultRootPage": "/reactiweb/index.html",
    "routesConfigFile": "config/routes.json"
}